package jpaadvance.repository;

import jpaadvance.entity.OTMFood;
import org.springframework.data.jpa.repository.JpaRepository;

public interface OTMFoodRepository extends JpaRepository<OTMFood, Long> {
}
