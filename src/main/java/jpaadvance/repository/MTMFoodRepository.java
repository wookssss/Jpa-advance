package jpaadvance.repository;

import jpaadvance.entity.MTMFood;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MTMFoodRepository extends JpaRepository<MTMFood, Long> {
}
