package jpaadvance.repository;

import jpaadvance.entity.MTOFood;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MTOFoodRepository extends JpaRepository<MTOFood, Long> {
}
